#include "async_double_pool_scheduler.h"
#include "naive_parallel_scheduler.h"

#include <gtest/gtest.h>

TEST(ScheduleTest, Basic) {
    // ara::schedule::AsyncDoublePoolScheduler scheduler;
    // ara::schedule::ScheduleContext schedule_context;
    // ara::task::Task task;
    // ara::task::TaskGroup task_group(task);
    // auto result = scheduler.Schedule(schedule_context, task_group);
    // ASSERT_TRUE(result.has_value());
    // auto handle = std::move(result.value());
    // auto future = handle->Wait(schedule_context);
    // ASSERT_TRUE(future.valid());
    // auto status = future.get();
    // ASSERT_TRUE(status.has_value());
    // ASSERT_EQ(status.value(), ara::task::TaskStatus::Finished());
}
